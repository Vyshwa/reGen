PORT=5000
# Local MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/regen

# To connect to MongoDB Atlas (Cloud), comment out the line above and uncomment the line below:
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/regen?retryWrites=true&w=majority
